# omnipackage configuration — generated by `omnipackage init`.
# Docs: https://docs.omnipackage.org
#
# REQUIRED EDITS before first build:
#   1. Generate a GPG key: omnipackage gpg generate -n "You" -e you@x --format base64
#      Put the printed value into GPG_KEY (e.g. in .env).
#   2. Configure at least one repository below.
#   3. Trim the `builds:` list to the distros you actually ship.
#
# Liquid tags ({{ ... }}, {% ... %}) render at *build* time — leave them as-is.

# Reads the version from Cargo.toml.
version_extractors:
  - name: default
    provider: "file"
    file:
      file: "__INIT_VERSION_FILE__"
      regex: '__INIT_VERSION_REGEX__'

common: &common
  package_name: "__INIT_PACKAGE_NAME__"
  maintainer: "__INIT_MAINTAINER__"
  homepage: __INIT_HOMEPAGE__
  description: __INIT_DESCRIPTION__

repositories:
  - name: Local test
    gpg_private_key_base64: "${GPG_KEY}"
    provider: localfs
    <<: *common
    localfs:
      path: "${HOME}/__INIT_PACKAGE_NAME__-repo"

  - name: Example bucket
    provider: s3
    gpg_private_key_base64: "${GPG_KEY}"
    <<: *common
    s3:
      bucket: example-bucket
      path_in_bucket: "__INIT_PACKAGE_NAME__"
      bucket_public_url: "https://example-bucket.your-domain.com"
      endpoint: "https://s3.your-provider.com"
      access_key_id: "${ACCESS_KEY_ID}"
      secret_access_key: "${SECRET_ACCESS_KEY}"
      region: auto
      force_path_style: true

# Most rpm distros ship a recent enough rust/cargo via the OS package manager.
# Older distros without rust use install_rust.sh to download the toolchain.
rpm: &rpm
  <<: *common
  rpm:
    spec_template: ".omnipackage/specfile.spec.liquid"

# Debian/Ubuntu's apt-cargo is often too old; use install_rust.sh per-distro below.
deb: &deb
  <<: *common
  deb:
    debian_templates: ".omnipackage/deb"

builds:
  - distro: "opensuse_15.3"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "opensuse_15.4"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "opensuse_15.5"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "opensuse_15.6"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "opensuse_16.0"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "opensuse_tumbleweed"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_38"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_39"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_40"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_41"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_42"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_43"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_44"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "fedora_rawhide"
    build_dependencies: ["rust", "cargo", "make"]
    <<: *rpm
  - distro: "debian_11"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "debian_12"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "debian_13"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "debian_testing"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "debian_unstable"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "ubuntu_20.04"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "ubuntu_22.04"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "ubuntu_24.04"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "ubuntu_25.04"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "ubuntu_26.04"
    build_dependencies: ["curl", "build-essential"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *deb
  - distro: "almalinux_8"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "almalinux_9"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "almalinux_10"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "rockylinux_8"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "rockylinux_9"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "mageia_9"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
  - distro: "mageia_cauldron"
    build_dependencies: ["curl", "gcc", "make"]
    before_build_script: ".omnipackage/install_rust.sh"
    <<: *rpm
