# PKGBUILD template rendered at build time. {{ vars }} come from config.yml.
# Maintainer: {{ maintainer }}
pkgname={{ package_name }}
pkgver={{ version }}
pkgrel=1
pkgdesc="{{ description }}"
arch=('x86_64')
url="{{ homepage }}"
license=('GPL-3.0-or-later')
{% if runtime_dependencies.size > 0 %}depends=({{ runtime_dependencies | join: ' ' }}){% endif %}
source=("{{ source_folder_name }}.tar.gz")
sha256sums=('SKIP')

build() {
  cd "$srcdir/{{ source_folder_name }}"
  go mod tidy
  go build -v -o __INIT_PACKAGE_NAME__
}

package() {
  cd "$srcdir/{{ source_folder_name }}"
  install -Dm755 __INIT_PACKAGE_NAME__ "$pkgdir/usr/bin/__INIT_PACKAGE_NAME__"
}
