<svg width="{{TOTAL_W}}" height="20" viewBox="0 0 {{TOTAL_W}} 20" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <clipPath id="bc"><rect width="{{TOTAL_W}}" height="20" rx="3"/></clipPath>
  </defs>

  <!-- left panel: logo + title -->
  <rect x="0" y="0" width="{{LEFT_W}}" height="20" fill="#3B3B38" clip-path="url(#bc)"/>

  <!-- right panel: aux -->
  <rect x="{{LEFT_W}}" y="0" width="{{RIGHT_W}}" height="20" fill="#3A6B1A" clip-path="url(#bc)"/>

  <!-- divider -->
  <line x1="{{LEFT_W}}" y1="0" x2="{{LEFT_W}}" y2="20" stroke="#555553" stroke-width="0.5"/>

  <!-- outer border -->
  <rect x="0.5" y="0.5" width="{{TOTAL_W_1}}" height="19" rx="2.5" fill="none" stroke="#222220" stroke-width="1" clip-path="url(#bc)"/>

  <!-- logo: scale=0.1428, ty=4, tx=5 -->
  <g transform="translate(5, 4) scale(0.1428)">
    <path d="M2 23.3l36.4-21 36.4 21v42l-36.4 21L2 65.4V23.3zm9.4 47.5l27 15.6 27-15.6V39.7l-27-15.6-27 15.6v31.1zm8.8 5.1l18.2 10.5 18.2-10.5v-21L38.4 44.5 20.2 54.9v21z"
          fill="none" stroke="#7CBF44" stroke-width="4" stroke-miterlimit="10"/>
  </g>

  <!-- title: x=18.4 (PAD_L + logo_w + GAP), vertically centered -->
  <text x="18.4" y="10" font-family="'Segoe UI', system-ui, sans-serif" font-size="11" font-weight="700" fill="#E8E6DF" dominant-baseline="central">{{TITLE}}</text>

  <!-- aux: centered in right panel -->
  <text x="{{AUX_CX}}" y="10" font-family="'Segoe UI', system-ui, sans-serif" font-size="11" font-weight="600" fill="#C8E6A0" text-anchor="middle" dominant-baseline="central">{{AUX}}</text>
</svg>
